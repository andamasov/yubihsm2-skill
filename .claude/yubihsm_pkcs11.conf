# YubiHSM 2 PKCS#11 module configuration.
#
# The yubihsm-pkcs11 module reads literal paths from this file; it
# does NOT expand environment variables or ~ . Set `cacert` to the
# absolute path of .claude/yubihsm-connector.crt in your local
# checkout of this repository before using.

connector = https://10.217.32.192:12345
cacert = /path/to/yubishm2-skill/.claude/yubihsm-connector.crt
